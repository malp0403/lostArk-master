import { InventoryHighlightPipe } from './inventory-highlight.pipe';

describe('InventoryHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new InventoryHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
